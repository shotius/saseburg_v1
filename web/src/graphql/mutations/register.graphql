mutation Register($registerCredentials: RegisterCredentials!) {
  register(credentials: $registerCredentials) {
    user {
     	firstName
      lastName
      email
    }
    errors {
      field
      message
    }
  }
}